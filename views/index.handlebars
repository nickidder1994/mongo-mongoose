<h1>Articles</h1>

{{dbArticle.length}}
<div class="container">
    <div class="row">
        <div class="col-md6">
            {{#each articles}}
            <p>{{title}}</p>
            <a href="{{link}}">{{link}}</a>
            {{/each}}
        </div>
    </div>
</div>